# `app/shared/diagnostics.py`

## Rol
Funciones compartidas para explicar como se obtuvo momentum y PTFAV paso a paso.

## Exporta
- `momentum_breakdown(closes)`
- `ptfav_trace(bars)`

## Uso principal
- Consumido por modulos `debug` y por `routes_chart.py`.
